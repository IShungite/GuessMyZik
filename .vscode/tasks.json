{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start frontend",
            "type": "shell",
            "command": "cd ${workspaceFolder}/frontend && docker compose up --build",
            "problemMatcher": []
        },
        {
            "label": "start backend",
            "type": "shell",
            "command": "cd ${workspaceFolder}/backend && docker compose up --build",
            "problemMatcher": []
        }
    ]
}